/**
 * Commitlint configuration
 * Extends the conventional commit configuration to enforce standard commit message formats.
 * Format: <type>(<scope>): <subject>
 */
module.exports = {
  extends: ['@commitlint/config-conventional'],
};
